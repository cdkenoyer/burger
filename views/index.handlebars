<h1>Burgers!</h1>

<h2>Burgers that are have not been Devoured!</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/cat-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that have been devoured!</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/cat-block sleep=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" burger_name="name">
  </div>

  <div class="form-group">
    <label for="devoured">Devoure?</label><br>
    <input type="radio" burger_name="sleepy" value="1" checked> Devoure!<br>
    <input type="radio" burger_name="sleepy" value="0"> Awake
  </div>

  <button type="submit">Add Burger</button>
</form>
